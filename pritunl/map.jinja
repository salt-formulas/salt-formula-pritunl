
{%- set source_engine = salt['pillar.get']('pritunl:server:source:engine') %}
Debian:
  pkgs:
  - pritunl
  service:
  - pritunl
{%- load_yaml as base_defaults %}
%- endload %}

{%- set server = salt['grains.filter_by'](base_defaults, merge=salt['pillar.get']('pritunl:server')) %}
